[project]
name = "agentic-tasks"
version = "0.1.0"
description = "WIP demo: AI agents planning a task checklist and updating it with tool calls"
authors = [
    { name = "Mikkaiser", email = "mikaelrsimoes19@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=3.19.2",
    "python-dotenv>=1.2.3",
    "rich>=15.0.0",
]

[tool.uv]
package = false
